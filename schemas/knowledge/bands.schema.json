{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "bands.schema.json",
  "title": "bands.json",
  "type": "array",
  "items": {
    "allOf": [
      {"$ref": "envelope.schema.json"},
      {
        "properties": {
          "category": {"const": "band_allocation"},
          "citations": {"minItems": 1}
        },
        "required": ["citations"]
      }
    ]
  }
}
