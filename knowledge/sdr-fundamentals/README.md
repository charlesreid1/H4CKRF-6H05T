# sdr-fundamentals/

Nyquist, aliasing, IQ imbalance, DC spike, gain staging (LNA/VGA/RF amp),
ADC quantization, and the `target_freq_hz` vs `center_freq_hz` trick
(documented in `src/hackrf_agent/ai/prompts.py:110`).

*Skeleton — Tier 1.*
