# inspectrum — precision spectrogram cursor tool

Read-only complement to URH. What URH does in "auto detect and edit,"
inspectrum does in "measure with a cursor, precisely." Two panes: a
scrollable spectrogram and a cursor overlay that reports frequency,
time, symbol rate.
