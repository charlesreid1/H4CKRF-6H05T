# bibliography — canonical sources

Prose companion to `../records/bibliography.json`. The JSON records
are machine-loaded by the MCP; this file gives the human-readable
context: which sources are canon, where to reach for each layer of
the stack, and pinpoint citations for the recurring authorities.
