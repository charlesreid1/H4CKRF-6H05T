# gqrx-cubicsdr-sdrpp — GUI receivers

Three interchangeable GUI SDR receivers: **gqrx** (Linux/BSD, GNU
Radio-backed), **CubicSDR** (cross-platform, LiquidDSP-backed),
**SDR++** (modern cross-platform contender). All three RX; none of
them commit TX by default. Pick by taste + platform.
